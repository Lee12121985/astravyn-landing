# Timesheet Buttons Standardization

## What we'll do

- Audit and align button sizes, spacing, and placement on the timesheet page (top toolbar, controls bar, action buttons, comment/chat icon).
- Apply a consistent style set (padding, radius, font weight, shadows) and align sections to reduce visual drift.

## Files to edit

- `timesheet/index.html` — adjust button styling in CSS and tweak layout alignment (top toolbar, month/year controls, action buttons).

## Steps

1) Normalize button styles (primary, flat/ghost, icon) with consistent padding, radius, font size, and min-width where appropriate.

2) Align toolbar layout: ensure even gaps, wrap handling, and consistent vertical centering.

3) Tidy controls row (month/year + action buttons) so widths and spacing are uniform; ensure mobile wrap behaves cleanly.